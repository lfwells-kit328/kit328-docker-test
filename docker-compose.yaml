services:
  frontend:
    build: ./frontend
    container_name: student-frontend
    networks:
      - caddy_network
    labels:
      # Caddy sees this and handles EVERYTHING (SSL + Routing)
      caddy: lfwells-kit328-test.gctrg-platform.cloud.edu.au
      caddy.reverse_proxy: "{{upstreams 3000}}"

networks:
  caddy_network:
    external: true